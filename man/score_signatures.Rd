% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scoring.R
\name{score_signatures}
\alias{score_signatures}
\title{Score gene signatures with the method used in their original publication}
\usage{
score_signatures(
  expr,
  signatures,
  methods = character(0),
  default_method = c("zmean", "gsva", "ssgsea"),
  min_genes = 3L
)
}
\arguments{
\item{expr}{Numeric matrix, genes x samples, on a log scale.}

\item{signatures}{Named list. Each element is either a character vector of genes,
or a list with `genes` and optional `weights`.}

\item{methods}{Named character vector giving the method per signature, one of
`"gsva"`, `"ssgsea"`, `"weighted"`, `"zmean"`. Missing entries default to
`default_method`.}

\item{default_method}{Method for signatures not named in `methods`.}

\item{min_genes}{Minimum genes that must be present for a signature to be scored.}
}
\value{
Numeric matrix, samples x signatures. Columns are `NA`-free; signatures
  with too few matched genes are dropped with a warning.
}
\description{
Signatures published with GSVA, ssGSEA or weighted-mean scoring are scored that
way rather than being forced into a single convention, because scoring method is
a common reviewer objection and the choice is cheap to respect.
}
