% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/benchmark.R
\name{benchmark_signatures}
\alias{benchmark_signatures}
\title{Benchmark signatures against a matched random-gene-set null}
\usage{
benchmark_signatures(
  expr,
  outcome,
  signatures,
  methods = character(0),
  default_method = "zmean",
  n_random = 300L,
  set_size = NULL,
  seed = 1L
)
}
\arguments{
\item{expr}{Genes x samples matrix, log scale.}

\item{outcome}{Either a binary vector (response; AUC is used) or a
[survival::Surv] object (C-index is used).}

\item{signatures}{Named list of gene sets (see [score_signatures()]).}

\item{methods, default_method}{Passed to [score_signatures()].}

\item{n_random}{Number of random gene sets.}

\item{set_size}{Size of each random set; defaults to the median signature size.}

\item{seed}{RNG seed.}
}
\value{
A data frame with one row per signature: the performance metric, the
  random-null median and 95th percentile, and whether the signature exceeds it.
}
\description{
The null draws random gene sets of the same size as the signature and allows the
direction of association to be chosen post hoc — the same latitude a published
signature receives in practice. This makes the null conservative rather than
lenient, which is the point: a signature that cannot clear it has not been shown
to carry information specific to the biology it names.
}
